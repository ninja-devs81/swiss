# Spitex Reconciliation Platform – Configuration
# Copy to .env and adjust values

# CRITICAL: Set a strong random secret in production
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME_TO_RANDOM_SECRET

# Session timeout in minutes
TOKEN_EXPIRE_MINUTES=120

# Max upload size in MB
MAX_UPLOAD_MB=50

# Users as JSON array with bcrypt-hashed passwords
# Generate hash: python -c "from passlib.context import CryptContext; c=CryptContext(schemes=['bcrypt']); print(c.hash('yourpassword'))"
USERS_JSON=[{"username":"admin","password":"$2b$12$LJ3m4ys3Lk0TSwHxWJN9aO3GZ9Jq1nFMXvq5W5oeYGJH3pZPkJXG","role":"admin"}]
